import AboutStylePage from "../components/AboutStylePage";

/** Uses merged defaults from `aboutStyleDefaults.js` — same layout as all About/guide pages */
export default function About() {
  return <AboutStylePage />;
}
